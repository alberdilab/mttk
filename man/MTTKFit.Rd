% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit.R
\name{MTTKFit}
\alias{MTTKFit}
\title{Construct an \code{MTTKFit}}
\usage{
MTTKFit(results = NULL, info = list(), models = list())
}
\arguments{
\item{results}{A \code{S4Vectors::DataFrame}, \code{data.frame}, or \code{NULL}.}

\item{info}{A named list describing the model specification and provenance.}

\item{models}{An optional named list of backend model objects.}
}
\value{
A valid \code{MTTKFit}.
}
\description{
\code{MTTKFit()} builds a compact result container for mttk modeling workflows.
The object behaves like an \code{S4Vectors::DataFrame}, with one row per modeled
feature and columns for coefficient summaries, fit diagnostics, and feature
metadata.
}
\examples{
fit <- MTTKFit(
    results = data.frame(
        ko_id = "K00001",
        estimate = 0.5,
        p_value = 0.01,
        row.names = "K00001"
    ),
    info = list(backend = "example")
)

fit
fitInfo(fit)$backend

}
