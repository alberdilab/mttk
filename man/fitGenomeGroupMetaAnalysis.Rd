% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit-meta-analysis.R
\name{fitGenomeGroupMetaAnalysis}
\alias{fitGenomeGroupMetaAnalysis}
\title{Meta-Analyze Genome Effects by a Genome Metadata Group}
\usage{
fitGenomeGroupMetaAnalysis(
  x,
  object,
  group,
  method = c("random", "fixed"),
  minGenomes = 1L,
  status = "ok",
  collapse = ";"
)
}
\arguments{
\item{x}{A genome-level \code{MTTKFit}, typically from \code{\link[=fitGenomeModel]{fitGenomeModel()}}.}

\item{object}{An \code{MTTKExperiment} supplying genome metadata.}

\item{group}{A single column name from \code{genomeData(object)} used to group
genomes, such as \code{"clade"} or \code{"domain"}.}

\item{method}{Meta-analysis method. \code{"random"} uses inverse-variance
random-effects meta-analysis with a DerSimonian-Laird \code{tau^2} estimate,
while \code{"fixed"} uses fixed-effect inverse-variance pooling.}

\item{minGenomes}{Minimum number of tested genome-level effects required for
a group to receive an \code{"ok"} result. Groups with fewer tested genomes are
returned with \code{status = "skipped"}.}

\item{status}{Optional genome-fit status filter applied before the
meta-analysis. The default keeps only genome rows with \code{status == "ok"}.
Use \code{NULL} to keep all genome rows.}

\item{collapse}{String used to combine genome identifiers in the
\code{member_genome_ids} and \code{tested_genome_ids} columns.}
}
\value{
An \code{MTTKFit} with one row per selected genome group.
}
\description{
\code{fitGenomeGroupMetaAnalysis()} summarizes genome-level effects to a grouping
variable stored in \code{genomeData(x)}, such as \code{clade}, \code{domain}, or another
taxonomic/phylogenetic grouping column.
}
\details{
The input \code{x} must be a genome-level \code{MTTKFit}, typically returned by
\code{\link[=fitGenomeModel]{fitGenomeModel()}}. Genome estimates and standard errors are combined within
each selected group using inverse-variance meta-analysis. By default a
random-effects model with a DerSimonian-Laird \code{tau^2} estimate is used.

This workflow is intended for the question: do the genomes assigned to a
given clade or taxonomic group show a coherent response, and how
heterogeneous are those genome-level responses?
}
\examples{
fit <- MTTKFit(
    results = data.frame(
        genome_id = c("genome_1", "genome_2", "genome_3"),
        estimate = c(0.5, 0.4, -0.2),
        std_error = c(0.2, 0.25, 0.3),
        status = c("ok", "ok", "ok"),
        row.names = c("genome_1", "genome_2", "genome_3")
    ),
    info = list(
        model = "genome_model",
        featureIdColumn = "genome_id",
        variable = "condition",
        variableType = "two_level_factor",
        referenceLevel = "control",
        contrastLevel = "treated",
        effectLabel = "condition: treated vs control"
    )
)

x <- makeExampleMTTKExperiment()
fitGenomeGroupMetaAnalysis(fit, x, group = "domain")

}
