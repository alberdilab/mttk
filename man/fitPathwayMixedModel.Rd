% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKExperiment-modeling-pathway.R
\name{fitPathwayMixedModel}
\alias{fitPathwayMixedModel}
\title{Fit a Pathway-Level Mixed Model}
\usage{
fitPathwayMixedModel(
  x,
  variable = NULL,
  formula = NULL,
  term = NULL,
  assay = "rna_gene_counts",
  libraryOffset = TRUE,
  genomeOffset = NULL,
  membershipMode = c("duplicate", "exclusive"),
  libSize = NULL,
  sampleBlock = NULL,
  genomeCorrelation = c("independent", "brownian"),
  phylogeny = NULL,
  genomeAssay = "dna_genome_counts",
  offsetPseudocount = 1,
  referenceLevels = NULL,
  keepFits = FALSE,
  BPPARAM = BiocParallel::SerialParam()
)
}
\arguments{
\item{x}{An \code{MTTKExperiment}.}

\item{variable}{A single sample-level column name from \code{colData(x)} for the
simple one-variable interface. The column must be numeric or a factor with
at least two levels. For factors with three or more levels supply \code{term} to
select which contrast to test. Supply exactly one of \code{variable} or \code{formula}.}

\item{formula}{Optional one-sided or two-sided fixed-effect formula for the
sample-level covariates, for example \code{~ condition + salinity} or
\code{rna_count ~ condition + salinity}. Offsets and genome-level random
effects are added internally by MTTK. Supply exactly one of \code{variable} or
\code{formula}.}

\item{term}{Optional character string naming the model term to test. Required
when \code{variable} refers to a factor with three or more levels (supply the
contrast name, e.g. \code{"conditionB"}) or when \code{formula} defines more than one
tested term.}

\item{assay}{Gene-level assay name used as the RNA response.}

\item{libraryOffset}{Logical; if \code{TRUE}, include \code{offset(log(lib_size))}.}

\item{genomeOffset}{Logical; if \code{TRUE}, include
\code{offset(log(genome_abundance + offsetPseudocount))}. If \code{NULL} (the
default), MTTK uses genome-abundance normalization when \code{genomeAssay} is
available in \code{genomeExperiment(x)}.}

\item{membershipMode}{How to handle many-to-many set memberships.
\code{"duplicate"} carries a gene's RNA into every mapped pathway, while
\code{"exclusive"} keeps only uniquely mapped genes.}

\item{libSize}{Library-size offset specification. Use \code{NULL} to compute
\code{colSums(rnaGeneCounts(x))}, a single \code{colData(x)} column name, or a
numeric vector with one value per sample.}

\item{sampleBlock}{Optional sample-level blocking variable from
\code{colData(x)}. When supplied, MTTK adds a random intercept
\code{(1 | sample_block)} to account for repeated measures or paired samples.}

\item{genomeCorrelation}{Genome-level correlation structure for the random
intercept. Use \code{"independent"} for the standard genome random intercept, or
\code{"brownian"} to model genome-level covariance from a phylogenetic tree via
\code{glmmTMB::propto()}.}

\item{phylogeny}{Optional \code{ape::phylo} object used when
\code{genomeCorrelation = "brownian"}. If \code{NULL}, MTTK uses \code{genomeTree(x)}.}

\item{genomeAssay}{Genome-level assay used when \code{genomeOffset = TRUE}.}

\item{offsetPseudocount}{Non-negative pseudocount added to genome abundance
before log-offset calculation.}

\item{referenceLevels}{Optional named list or named character vector setting
the reference levels of factor-like sample covariates before model fitting.}

\item{keepFits}{Logical; if \code{TRUE}, store the backend \code{glmmTMB} model objects
in the returned \code{MTTKFit}.}

\item{BPPARAM}{A \code{BiocParallelParam} instance controlling how models are
fitted in parallel. Defaults to \code{BiocParallel::SerialParam()} for
single-core execution. Pass e.g. \code{BiocParallel::MulticoreParam(4)} to fit
models in parallel.}
}
\value{
An \code{MTTKFit} with one row per pathway.
}
\description{
\code{fitPathwayMixedModel()} fits one negative-binomial mixed model per pathway
using \code{glmmTMB}. Gene-level RNA counts are first aggregated to
pathway-within-genome observations by following \code{gene_to_ko -> ko_to_pathway},
then each pathway is modeled across genomes and samples.
}
\details{
This workflow is intended for the total-activity question: does the summed
RNA assigned to a pathway shift across samples after accounting for genome
structure and optional genome abundance? For the different question of
whether the KO-level effects assigned to a pathway show a coherent direction
of change, use \code{\link[=fitPathwayMetaAnalysis]{fitPathwayMetaAnalysis()}} instead.

\code{membershipMode = "duplicate"} answers that question in terms of all RNA
assigned to the pathway, even if the same KO contributes to multiple
pathways. \code{membershipMode = "exclusive"} instead restricts the analysis to
uniquely assigned memberships, which avoids duplicated counts but may remove
many genes when pathway overlap is dense.

\code{libraryOffset} controls whether the model includes
\code{offset(log(lib_size))}. \code{genomeOffset} controls whether the model includes
\code{offset(log(genome_abundance + offsetPseudocount))}. The genome random
effect \code{(1 | genome_id)} is part of this workflow regardless of the offset
settings.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeExampleMTTKExperiment()
    fit <- fitPathwayMixedModel(x, variable = "condition")
    fit
}

}
