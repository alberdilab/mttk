% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit-class.R
\name{MTTKFit-class}
\alias{MTTKFit-class}
\title{MTTKFit Class}
\description{
\code{MTTKFit} stores feature-level model results as a Bioconductor-compatible
table. Each row represents one modeled feature, such as a KO, and the
columns store coefficient summaries, fit diagnostics, and feature-level
metadata.
}
\details{
The class extends \link[S4Vectors:DataFrame-class]{S4Vectors::DFrame}, so it behaves like a regular
\code{S4Vectors::DataFrame}. Additional fit metadata is stored in
\code{metadata(x)$mttk_fit}.
}
\section{Canonical identifiers}{

\code{rownames(x)} are the canonical identifiers of the modeled features. When
the modeled feature ID is also stored in a result column such as \code{ko_id},
\code{gene_id}, \code{genome_id}, \code{module_id}, or \code{pathway_id}, that canonical ID
column must match \code{rownames(x)} exactly.
}

\section{Stored metadata}{

\code{metadata(x)$mttk_fit} is reserved for MTTK-specific fit metadata and may
contain:
\itemize{
\item \code{info}: a named list describing the model specification, backend, and
provenance.
\item \code{models}: an optional named list of backend model objects.
}
}

