% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit-phylogeny.R
\name{fitModulePhylogeneticGLS}
\alias{fitModulePhylogeneticGLS}
\title{Fit a Module-Level Phylogenetic GLS Model}
\usage{
fitModulePhylogeneticGLS(
  x,
  object,
  tree = NULL,
  method = c("REML", "ML"),
  useWeights = TRUE,
  minGenomes = 3L,
  status = "ok",
  keepFits = FALSE
)
}
\arguments{
\item{x}{A module random-slope \code{MTTKFit}, from \code{\link[=fitModuleRandomSlopeModel]{fitModuleRandomSlopeModel()}}.}

\item{object}{An \code{MTTKExperiment} supplying \code{genomeTree(object)}.}

\item{tree}{Optional \code{ape::phylo} object. When \code{NULL}, \code{genomeTree(object)}
is used.}

\item{method}{Fitting method passed to \code{nlme::gls()}.}

\item{useWeights}{Logical; if \code{TRUE}, the phylogenetic GLS uses the fitted
effect standard errors as inverse-variance weights.}

\item{minGenomes}{Minimum number of genomes with finite feature effects
required for a KO to receive an \code{"ok"} result.}

\item{status}{Optional genome-fit status filter applied before fitting. The
default keeps only genome rows with \code{status == "ok"}.}

\item{keepFits}{Logical; should the backend \code{gls} fit object be stored in the
returned \code{MTTKFit}?}
}
\value{
An \code{MTTKFit} with one row per module.
}
\description{
\code{fitModulePhylogeneticGLS()} fits one intercept-only phylogenetic GLS model
per module using the genome-specific module effects returned by
\code{\link[=fitModuleRandomSlopeModel]{fitModuleRandomSlopeModel()}}.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeShowcaseMTTKExperiment()
    module_fit <- fitModuleRandomSlopeModel(x, variable = "condition")
    fitModulePhylogeneticGLS(module_fit, x)
}

}
