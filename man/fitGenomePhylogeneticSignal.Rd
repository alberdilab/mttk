% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit-phylogeny.R
\name{fitGenomePhylogeneticSignal}
\alias{fitGenomePhylogeneticSignal}
\title{Test for Overall Phylogenetic Signal in Genome-Level Responses}
\usage{
fitGenomePhylogeneticSignal(
  x,
  object,
  tree = NULL,
  correlationMethod = c("spearman", "pearson"),
  alternative = c("two.sided", "greater", "less"),
  nPerm = 999L,
  status = "ok"
)
}
\arguments{
\item{x}{A genome-level \code{MTTKFit}, typically returned by \code{\link[=fitGenomeModel]{fitGenomeModel()}}.}

\item{object}{An \code{MTTKExperiment} supplying \code{genomeTree(object)}.}

\item{tree}{Optional \code{ape::phylo} object. When \code{NULL}, \code{genomeTree(object)}
is used.}

\item{correlationMethod}{Correlation used to quantify the association
between phylogenetic distance and response difference.}

\item{alternative}{Alternative hypothesis for the permutation p-value.}

\item{nPerm}{Number of tip-label permutations used to estimate the p-value.}

\item{status}{Optional genome-fit status filter applied before the signal
test. The default keeps only genome rows with \code{status == "ok"}.}
}
\value{
An \code{MTTKFit} with one row summarizing the overall phylogenetic
signal in the genome-level responses.
}
\description{
\code{fitGenomePhylogeneticSignal()} follows \code{\link[=fitGenomeModel]{fitGenomeModel()}} with a tree-based
summary of whether more closely related genomes tend to show more similar
fitted responses.
}
\details{
It tests the association between pairwise phylogenetic distances and
pairwise absolute response differences across genomes. Positive statistics
indicate that more distantly related genomes tend to differ more strongly in
their fitted response estimates.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeShowcaseMTTKExperiment()
    genome_fit <- fitGenomeModel(x, variable = "condition")
    fitGenomePhylogeneticSignal(genome_fit, x, nPerm = 99)
}

}
