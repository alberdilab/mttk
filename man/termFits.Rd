% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit.R
\name{termFits}
\alias{termFits}
\title{Split a Multi-Term Fit Into Per-Term \code{MTTKFit} Objects}
\usage{
termFits(x, terms = NULL)
}
\arguments{
\item{x}{An \code{MTTKFit} containing stored coefficient summaries.}

\item{terms}{Optional character vector of terms to materialize. When \code{NULL},
all non-intercept fixed-effect terms are returned.}
}
\value{
A named list of \code{MTTKFit} objects, one per requested term.
}
\description{
\code{termFits()} materializes one canonical \code{MTTKFit} per fixed-effect term
stored inside a multivariable fit. This allows users to fit the model once
and then inspect each coefficient as its own \code{MTTKFit} object without
refitting.
}
\details{
By default the returned list excludes the intercept term. Request specific
terms through \verb{terms =}, using the same simplified matching supported by
\code{\link[=coefTable]{coefTable()}} and \code{\link[=fitTable]{fitTable()}}, such as \code{"salinity"} or
\code{"condition:salinity"}.

Backend model objects are reused across the returned fits. Stored
random-slope conditional effects are retained only for the split
corresponding to the original focal term of \code{x}, because those conditional
effects are term-specific.
}
\examples{
fit <- MTTKFit(
    results = data.frame(
        ko_id = c("K1", "K2"),
        tested_term = c("conditiontreated", "conditiontreated"),
        estimate = c(0.3, -0.2),
        row.names = c("K1", "K2")
    ),
    info = list(
        featureIdColumn = "ko_id",
        testedTerm = "conditiontreated"
    ),
    coefficients = data.frame(
        ko_id = c("K1", "K1", "K2", "K2"),
        model_term = c("conditiontreated", "salinity", "conditiontreated", "salinity"),
        term_variable = c("condition", "salinity", "condition", "salinity"),
        term_variable_type = c("two_level_factor", "numeric", "two_level_factor", "numeric"),
        term_effect_label = c(
            "condition: treated vs control",
            "salinity per unit increase",
            "condition: treated vs control",
            "salinity per unit increase"
        ),
        estimate = c(0.3, 0.1, -0.2, -0.4),
        std_error = c(0.1, 0.05, 0.1, 0.06),
        p_value = c(0.02, 0.10, 0.05, 0.01),
        row.names = c("K1::conditiontreated", "K1::salinity", "K2::conditiontreated", "K2::salinity")
    )
)

names(termFits(fit))

}
