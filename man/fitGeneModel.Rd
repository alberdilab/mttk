% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKExperiment-modeling.R
\name{fitGeneModel}
\alias{fitGeneModel}
\title{Fit a Gene-Level Model}
\usage{
fitGeneModel(
  x,
  variable,
  model = c("library_only", "library_plus_genome_abundance"),
  assay = "rna_gene_counts",
  libSize = NULL,
  genomeAssay = "dna_genome_counts",
  offsetPseudocount = 1,
  keepFits = FALSE
)
}
\arguments{
\item{x}{An \code{MTTKExperiment}.}

\item{variable}{A single sample-level column name from \code{colData(x)}. The
column must be numeric or a factor with exactly two levels.}

\item{model}{Which of the two supported gene-level model specifications to
fit.}

\item{assay}{Gene-level assay name used as the RNA response.}

\item{libSize}{Library-size offset specification. Use \code{NULL} to compute
\code{colSums(rnaGeneCounts(x))}, a single \code{colData(x)} column name, or a
numeric vector with one value per sample.}

\item{genomeAssay}{Genome-level assay used when
\code{model = "library_plus_genome_abundance"}.}

\item{offsetPseudocount}{Non-negative pseudocount added to genome abundance
before log-offset calculation.}

\item{keepFits}{Logical; if \code{TRUE}, store the backend \code{glmmTMB} model objects
in the returned \code{MTTKFit}.}
}
\value{
An \code{MTTKFit} with one row per gene.
}
\description{
\code{fitGeneModel()} fits one negative-binomial model per gene using \code{glmmTMB}.
This workflow is intended for the gene-level question of which individual
genes change across conditions or are associated with a continuous variable.
}
\details{
The first implementation supports two model specifications:
\itemize{
\item \code{"library_only"} fits
\code{rna_count ~ variable + offset(log(lib_size))}
\item \code{"library_plus_genome_abundance"} fits
\code{rna_count ~ variable + offset(log(lib_size)) + offset(log(genome_abundance + offsetPseudocount))}
}

In this workflow the parent genome is handled through the optional
genome-abundance offset rather than as a random effect, because each gene is
permanently assigned to one genome.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeExampleMTTKExperiment()
    fit <- fitGeneModel(x, variable = "condition")
    fit
    fitInfo(fit)$effectLabel
    as.data.frame(fit)[, c("gene_id", "genome_id", "effect_label", "estimate", "q_value")]
}

}
