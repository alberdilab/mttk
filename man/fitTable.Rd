% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit.R
\name{fitTable}
\alias{fitTable}
\title{Extract a Sorted Fit Result Table}
\usage{
fitTable(
  x,
  status = NULL,
  term = NULL,
  sortBy = NULL,
  decreasing = NULL,
  n = NULL
)
}
\arguments{
\item{x}{An \code{MTTKFit}.}

\item{status}{Optional character vector of fit statuses to keep, such as
\code{"ok"} or \code{c("ok", "skipped")}. Use \code{NULL} to keep all rows.}

\item{term}{Optional fitted model term to report instead of the canonical
focal term stored in the fit. This requires stored coefficient summaries
and can be useful for inspecting secondary coefficients from a multivariable
formula fit.}

\item{sortBy}{Optional sort key. This can be any result column name or the
special value \code{"abs_estimate"}.}

\item{decreasing}{Logical or \code{NULL}. When \code{NULL}, p-values and q-values sort
in ascending order and other keys sort in descending order.}

\item{n}{Optional maximum number of rows to return after filtering and
sorting.}
}
\value{
An \code{S4Vectors::DataFrame}.
}
\description{
\code{fitTable()} returns an ordered \code{S4Vectors::DataFrame} view of an
\code{MTTKFit}. This is useful when the fit should be inspected as a regular
result table, optionally filtered to a subset of fit statuses and sorted by
p-values, q-values, effect size, or another result column.
}
\examples{
fit <- MTTKFit(
    results = data.frame(
        ko_id = c("K1", "K2"),
        estimate = c(0.3, -1.2),
        q_value = c(0.2, 0.01),
        status = c("ok", "ok"),
        row.names = c("K1", "K2")
    )
)

fitTable(fit, sortBy = "q_value")

}
