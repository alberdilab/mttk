% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKExperiment-modeling.R
\name{fitKOMixedModel}
\alias{fitKOMixedModel}
\title{Fit a KO-Level Mixed Model}
\usage{
fitKOMixedModel(
  x,
  variable = NULL,
  formula = NULL,
  term = NULL,
  assay = "rna_gene_counts",
  libraryOffset = TRUE,
  genomeOffset = NULL,
  libSize = NULL,
  genomeAssay = "dna_genome_counts",
  offsetPseudocount = 1,
  referenceLevels = NULL,
  keepFits = FALSE
)
}
\arguments{
\item{x}{An \code{MTTKExperiment}.}

\item{variable}{A single sample-level column name from \code{colData(x)} for the
simple one-variable interface. The column must be numeric or a factor with
exactly two levels. Supply exactly one of \code{variable} or \code{formula}.}

\item{formula}{Optional one-sided or two-sided fixed-effect formula for the
sample-level covariates, for example \code{~ condition + salinity} or
\code{rna_count ~ condition + salinity}. Offsets and genome-level random
effects are added internally by MTTK. Supply exactly one of \code{variable} or
\code{formula}.}

\item{term}{Optional fixed-effect term to extract from a formula-based fit.
This is required when the fixed-effect formula defines more than one tested
term.}

\item{assay}{Gene-level assay name used as the RNA response.}

\item{libraryOffset}{Logical; if \code{TRUE}, include \code{offset(log(lib_size))}.}

\item{genomeOffset}{Logical; if \code{TRUE}, include
\code{offset(log(genome_abundance + offsetPseudocount))}. If \code{NULL} (the
default), MTTK uses genome-abundance normalization when \code{genomeAssay} is
available in \code{genomeExperiment(x)}.}

\item{libSize}{Library-size offset specification. Use \code{NULL} to compute
\code{colSums(rnaGeneCounts(x))}, a single \code{colData(x)} column name, or a
numeric vector with one value per sample.}

\item{genomeAssay}{Genome-level assay used when \code{genomeOffset = TRUE}.}

\item{offsetPseudocount}{Non-negative pseudocount added to genome abundance
before log-offset calculation.}

\item{referenceLevels}{Optional named list or named character vector setting
the reference levels of factor-like sample covariates before model fitting.}

\item{keepFits}{Logical; if \code{TRUE}, store the backend \code{glmmTMB} model objects
in the returned \code{MTTKFit}.}
}
\value{
An \code{MTTKFit} with one row per KO.
}
\description{
\code{fitKOMixedModel()} fits one negative-binomial mixed model per KO using
\code{glmmTMB}. RNA counts are first aggregated to KO-within-genome observations,
so the fitted model respects the nesting of genes within genomes.
}
\details{
\code{libraryOffset} controls whether the model includes
\code{offset(log(lib_size))}. \code{genomeOffset} controls whether the model includes
\code{offset(log(genome_abundance + offsetPseudocount))}. The genome random
effect \code{(1 | genome_id)} is part of this workflow regardless of the offset
settings.

This workflow is intended for KO-level differential expression or association
analysis, where each KO can be observed in multiple genomes and the genome
term should be modeled explicitly as a random intercept.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeExampleMTTKExperiment()
    fit <- fitKOMixedModel(x, variable = "condition")
    fit
    significantResults(fit)
}

}
