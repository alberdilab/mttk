% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit.R
\name{significantResults}
\alias{significantResults}
\title{Filter Significant Fit Results}
\usage{
significantResults(
  x,
  alpha = 0.05,
  value = c("q_value", "p_value"),
  status = "ok",
  direction = c("both", "up", "down"),
  n = NULL,
  sortBy = NULL,
  decreasing = NULL
)
}
\arguments{
\item{x}{An \code{MTTKFit}.}

\item{alpha}{Significance threshold.}

\item{value}{Which significance column to use, typically \code{"q_value"} or
\code{"p_value"}.}

\item{status}{Optional fit status to keep before significance filtering.}

\item{direction}{Optional effect-direction filter. \code{"up"} keeps positive
estimates, \code{"down"} keeps negative estimates, and \code{"both"} keeps both.}

\item{n}{Optional maximum number of rows to return after filtering and
sorting.}

\item{sortBy}{Optional sort key. When \code{NULL}, the significance column in
\code{value} is used.}

\item{decreasing}{Logical or \code{NULL}, passed to the sorting step.}
}
\value{
An \code{MTTKFit}.
}
\description{
\code{significantResults()} returns the subset of an \code{MTTKFit} that passes a
significance threshold. By default it uses \code{q_value <= 0.05} and keeps only
rows with \code{status == "ok"}.
}
\details{
The returned object is still an \code{MTTKFit}, so stored model objects and fit
metadata remain aligned to the retained rows.
}
\examples{
fit <- MTTKFit(
    results = data.frame(
        ko_id = c("K1", "K2"),
        estimate = c(0.3, -1.2),
        q_value = c(0.2, 0.01),
        status = c("ok", "ok"),
        row.names = c("K1", "K2")
    )
)

significantResults(fit)

}
