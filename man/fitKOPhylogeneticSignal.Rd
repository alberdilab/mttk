% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit-phylogeny.R
\name{fitKOPhylogeneticSignal}
\alias{fitKOPhylogeneticSignal}
\title{Test for KO-Level Phylogenetic Signal in Genome-Specific Responses}
\usage{
fitKOPhylogeneticSignal(
  x,
  object,
  tree = NULL,
  correlationMethod = c("spearman", "pearson"),
  alternative = c("two.sided", "greater", "less"),
  nPerm = 999L,
  minGenomes = 3L,
  status = "ok"
)
}
\arguments{
\item{x}{A KO random-slope \code{MTTKFit}, from \code{\link[=fitKORandomSlopeModel]{fitKORandomSlopeModel()}}.}

\item{object}{An \code{MTTKExperiment} supplying \code{genomeTree(object)}.}

\item{tree}{Optional \code{ape::phylo} object. When \code{NULL}, \code{genomeTree(object)}
is used.}

\item{correlationMethod}{Correlation used to quantify the association
between phylogenetic distance and response difference.}

\item{alternative}{Alternative hypothesis for the permutation p-value.}

\item{nPerm}{Number of tip-label permutations used to estimate the p-value.}

\item{minGenomes}{Minimum number of genomes with finite KO genome effects
required for a KO to receive an \code{"ok"} result.}

\item{status}{Optional KO-fit status filter applied before the signal test.
The default keeps only KO rows with \code{status == "ok"}.}
}
\value{
An \code{MTTKFit} with one row per KO.
}
\description{
\code{fitKOPhylogeneticSignal()} follows \code{\link[=fitKORandomSlopeModel]{fitKORandomSlopeModel()}} with a
tree-based summary for each KO. It asks whether genome-specific KO response
estimates tend to be more similar among closely related genomes.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeShowcaseMTTKExperiment()
    ko_fit <- fitKORandomSlopeModel(x, variable = "condition")
    fitKOPhylogeneticSignal(ko_fit, x, nPerm = 99)
}

}
