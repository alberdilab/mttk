% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKExperiment-analysis.R
\name{aggregateToGenome}
\alias{aggregateToGenome}
\title{Aggregate Assays to the Genome Level}
\usage{
aggregateToGenome(x, assays = NULL, fun = c("sum", "mean"))
}
\arguments{
\item{x}{An \code{MTTKExperiment}.}

\item{assays}{Character vector of assay names to aggregate. The default uses
all assays in \code{x}.}

\item{fun}{Aggregation function. Supported values are \code{"sum"} and \code{"mean"}.}
}
\value{
A \code{SummarizedExperiment} with one row per genome.
}
\description{
\code{aggregateToGenome()} is a convenience wrapper around \code{aggregateByLink()} for
the common case of aggregating gene-level measurements to genomes through the
core \code{rowData(x)$genome_id} mapping. When an input assay name contains \code{"_gene_"},
the returned assay name is rewritten with \code{"_genome_"} so the level of the
aggregated data remains explicit.
}
\examples{
x <- makeExampleMTTKExperiment()
aggregateToGenome(x)

}
