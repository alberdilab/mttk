% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit-phylogeny.R
\name{scanPathwayClades}
\alias{scanPathwayClades}
\title{Scan a Genome Phylogeny for Pathway-Specific Responding Clades}
\usage{
scanPathwayClades(
  x,
  object,
  tree = NULL,
  alternative = c("two.sided", "greater", "less"),
  nPerm = 999L,
  minTips = 2L,
  maxTips = NULL,
  minGenomes = 3L,
  status = "ok"
)
}
\arguments{
\item{x}{A pathway random-slope \code{MTTKFit}, from \code{\link[=fitPathwayRandomSlopeModel]{fitPathwayRandomSlopeModel()}}.}

\item{object}{An \code{MTTKExperiment} supplying \code{genomeTree(object)}.}

\item{tree}{Optional rooted \code{ape::phylo} object. When \code{NULL},
\code{genomeTree(object)} is used.}

\item{alternative}{Alternative hypothesis for the permutation p-value.}

\item{nPerm}{Number of tip-label permutations used to estimate the p-value.}

\item{minTips}{Minimum subtree size to test.}

\item{maxTips}{Optional maximum subtree size to test.}

\item{minGenomes}{Minimum number of genomes with finite KO effects required
before clades are scanned for a KO.}

\item{status}{Optional KO-fit status filter applied before the clade scan.
The default keeps only KO rows with \code{status == "ok"}.}
}
\value{
An \code{MTTKFit} with one row per pathway/clade combination.
}
\description{
\code{scanPathwayClades()} follows \code{\link[=fitPathwayRandomSlopeModel]{fitPathwayRandomSlopeModel()}} with a subtree
scan for each pathway. Each internal clade in the rooted genome phylogeny is
tested for a shifted mean pathway genome effect relative to the complement of
that clade.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeShowcaseMTTKExperiment()
    pathway_fit <- fitPathwayRandomSlopeModel(x, variable = "condition")
    scanPathwayClades(pathway_fit, x, nPerm = 99, minTips = 2)
}

}
