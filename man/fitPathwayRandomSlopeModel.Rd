% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKExperiment-modeling.R
\name{fitPathwayRandomSlopeModel}
\alias{fitPathwayRandomSlopeModel}
\title{Fit a Pathway-Level Random-Slope Mixed Model}
\usage{
fitPathwayRandomSlopeModel(
  x,
  variable = NULL,
  formula = NULL,
  term = NULL,
  assay = "rna_gene_counts",
  libraryOffset = TRUE,
  genomeOffset = NULL,
  membershipMode = c("duplicate", "exclusive"),
  libSize = NULL,
  genomeAssay = "dna_genome_counts",
  offsetPseudocount = 1,
  referenceLevels = NULL,
  randomSlope = NULL,
  keepFits = FALSE
)
}
\arguments{
\item{x}{An \code{MTTKExperiment}.}

\item{variable}{A single sample-level column name from \code{colData(x)} for the
simple one-variable interface. The column must be numeric or a factor with
exactly two levels. Supply exactly one of \code{variable} or \code{formula}.}

\item{formula}{Optional one-sided or two-sided fixed-effect formula for the
sample-level covariates, for example \code{~ condition + salinity} or
\code{rna_count ~ condition + salinity}. Offsets and genome-level random
effects are added internally by MTTK. Supply exactly one of \code{variable} or
\code{formula}.}

\item{term}{Optional fixed-effect term to extract from a formula-based fit.
This is required when the fixed-effect formula defines more than one tested
term.}

\item{assay}{Gene-level assay name used as the RNA response.}

\item{libraryOffset}{Logical; if \code{TRUE}, include \code{offset(log(lib_size))}.}

\item{genomeOffset}{Logical; if \code{TRUE}, include
\code{offset(log(genome_abundance + offsetPseudocount))}. If \code{NULL} (the
default), MTTK uses genome-abundance normalization when \code{genomeAssay} is
available in \code{genomeExperiment(x)}.}

\item{membershipMode}{How to handle many-to-many set memberships.
\code{"duplicate"} carries a gene's RNA into every mapped pathway, while
\code{"exclusive"} keeps only uniquely mapped genes.}

\item{libSize}{Library-size offset specification. Use \code{NULL} to compute
\code{colSums(rnaGeneCounts(x))}, a single \code{colData(x)} column name, or a
numeric vector with one value per sample.}

\item{genomeAssay}{Genome-level assay used when \code{genomeOffset = TRUE}.}

\item{offsetPseudocount}{Non-negative pseudocount added to genome abundance
before log-offset calculation.}

\item{referenceLevels}{Optional named list or named character vector setting
the reference levels of factor-like sample covariates before model fitting.}

\item{randomSlope}{Optional sample-level variable whose genome-specific slope
should be estimated. When \code{variable} is supplied, MTTK uses that variable.
When \code{formula} is supplied, \code{randomSlope} can be omitted only if MTTK can
infer a single tested variable unambiguously.}

\item{keepFits}{Logical; if \code{TRUE}, store the backend \code{glmmTMB} model objects
in the returned \code{MTTKFit}.}
}
\value{
An \code{MTTKFit} with one row per pathway. The returned fit also stores
pathway-by-genome conditional effects that can be extracted with
\code{\link[=pathwayGenomeEffects]{pathwayGenomeEffects()}}.
}
\description{
\code{fitPathwayRandomSlopeModel()} fits one negative-binomial mixed model per
pathway using \code{glmmTMB}, with both a genome random intercept and a
genome-specific random slope for the selected sample variable.
}
\details{
Gene-level RNA counts are first aggregated to pathway-within-genome
observations by following \code{gene_to_ko -> ko_to_pathway}. The fitted model
then estimates an overall pathway-level effect together with genome-specific
deviations around that pathway effect.

This workflow is intended for the question: does the same pathway show
similar or heterogeneous condition-associated responses across genomes? For
the different higher-level question of whether KO-level pathway members show
a coherent direction of change without re-aggregating counts, use
\code{\link[=fitPathwayMetaAnalysis]{fitPathwayMetaAnalysis()}} instead.

\code{membershipMode = "duplicate"} answers that question in terms of all RNA
assigned to the pathway, even if the same KO contributes to multiple
pathways. \code{membershipMode = "exclusive"} instead restricts the analysis to
uniquely assigned memberships.

Genome-specific conditional pathway effects can be extracted with
\code{\link[=pathwayGenomeEffects]{pathwayGenomeEffects()}}.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeExampleMTTKExperiment()
    fit <- fitPathwayRandomSlopeModel(x, variable = "condition")
    fit
    pathwayGenomeEffects(fit)
}

}
