% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit-meta-analysis.R
\name{fitPathwayMetaAnalysis}
\alias{fitPathwayMetaAnalysis}
\title{Meta-Analyze KO Effects to the Pathway Level}
\usage{
fitPathwayMetaAnalysis(
  x,
  object,
  method = c("random", "fixed"),
  membershipMode = c("duplicate", "split", "exclusive"),
  minKOs = 1L,
  status = "ok",
  collapse = ";"
)
}
\arguments{
\item{x}{A KO-level \code{MTTKFit}, typically from \code{\link[=fitKOMixedModel]{fitKOMixedModel()}} or
\code{\link[=fitKORandomSlopeModel]{fitKORandomSlopeModel()}}.}

\item{object}{An \code{MTTKExperiment} supplying \code{ko_to_module} memberships.}

\item{method}{Meta-analysis method. \code{"random"} uses inverse-variance
random-effects meta-analysis with a DerSimonian-Laird \code{tau^2} estimate,
while \code{"fixed"} uses fixed-effect inverse-variance pooling.}

\item{membershipMode}{How to handle many-to-many KO memberships.
\code{"duplicate"} carries each KO fully into every mapped set,
\code{"split"} divides each KO's contribution across its memberships, and
\code{"exclusive"} keeps only uniquely assigned KOs.}

\item{minKOs}{Minimum number of tested KO-level effects required for a module
to receive an \code{"ok"} result. Modules with fewer tested KOs are returned
with \code{status = "skipped"}.}

\item{status}{Optional KO-fit status filter applied before the meta-analysis.
The default keeps only KO rows with \code{status == "ok"}. Use \code{NULL} to keep
all KO rows.}

\item{collapse}{String used to combine KO identifiers in the
\code{member_ko_ids} and \code{tested_ko_ids} columns.}
}
\value{
An \code{MTTKFit} with one row per pathway.
}
\description{
\code{fitPathwayMetaAnalysis()} summarizes KO-level effects to pathway-level
associations without refitting the count model. This workflow is intended for
the question: do the KOs assigned to a KEGG pathway show a coherent
condition-associated shift?
}
\details{
The input \code{x} must be a KO-level \code{MTTKFit}, typically returned by
\code{\link[=fitKOMixedModel]{fitKOMixedModel()}} or \code{\link[=fitKORandomSlopeModel]{fitKORandomSlopeModel()}}. KO estimates and standard
errors are combined within each pathway using inverse-variance meta-analysis.
By default a random-effects model with a DerSimonian-Laird \code{tau^2} estimate
is used.

Because KEGG pathway membership is many-to-many, the same KO can contribute
its KO-level effect to multiple pathways. This workflow therefore answers a
different question from \code{\link[=fitPathwayMixedModel]{fitPathwayMixedModel()}}, which re-aggregates counts
and tests shifts in total pathway activity.

The interpretation of \code{membershipMode} is the same as in
\code{\link[=fitModuleMetaAnalysis]{fitModuleMetaAnalysis()}}: use \code{"duplicate"} for annotation-centric
synthesis, \code{"split"} to divide each KO contribution across memberships, and
\code{"exclusive"} when only uniquely assigned KOs should contribute.

The current implementation treats KO-level effects as approximately
independent, so higher-level p-values should be interpreted as an
approximate synthesis layer rather than as a fully correlation-aware test.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeExampleMTTKExperiment()
    ko_fit <- fitKOMixedModel(x, variable = "condition")
    pathway_fit <- fitPathwayMetaAnalysis(ko_fit, x)
    as.data.frame(pathway_fit)[, c("pathway_id", "estimate", "q_value", "n_kos_tested")]
}

}
