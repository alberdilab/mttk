% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit-meta-analysis.R
\name{compareKOGenomeGroups}
\alias{compareKOGenomeGroups}
\title{Compare KO Genome-Group Responses Between Groups}
\usage{
compareKOGenomeGroups(x, reference = NULL, contrast = NULL, status = "ok")
}
\arguments{
\item{x}{An \code{MTTKFit} returned by \code{\link[=fitKOGenomeGroupMetaAnalysis]{fitKOGenomeGroupMetaAnalysis()}}.}

\item{reference}{Optional single reference group identifier. When \code{NULL} and
\code{contrast = NULL}, all pairwise group comparisons are returned. When
\code{reference} is supplied and \code{contrast = NULL}, MTTK compares that
reference group against every other available group.}

\item{contrast}{Optional single contrast group identifier. When supplied, a
single directed contrast \code{contrast - reference} is returned.}

\item{status}{Optional fit-status filter applied to the input KO/group rows
before comparison. The default keeps only rows with \code{status == "ok"}. Use
\code{NULL} to keep all KO/group rows.}
}
\value{
An \code{MTTKFit} with one row per KO/group comparison.
}
\description{
\code{compareKOGenomeGroups()} follows \code{\link[=fitKOGenomeGroupMetaAnalysis]{fitKOGenomeGroupMetaAnalysis()}} with a
direct between-group comparison step. It compares the KO-level group
meta-estimates for two groups and tests whether the estimated KO response
differs between them.
}
\details{
This workflow is intended for the question: for a given KO, is the
group-level response different between two clades, domains, or other
genome-derived groups?

The input \code{x} must be an \code{MTTKFit} returned by
\code{\link[=fitKOGenomeGroupMetaAnalysis]{fitKOGenomeGroupMetaAnalysis()}}. The comparison estimate is calculated as
\code{contrast - reference}, using the group-level KO meta-estimates stored in the
input fit. The comparison standard error is computed as
\code{sqrt(se_contrast^2 + se_reference^2)}, which is appropriate when the two
compared groups contain disjoint genomes.

Because the input group-level KO fits are themselves approximate summaries of
genome-specific conditional effects, the resulting between-group p-values
should also be interpreted as approximate.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeExampleMTTKExperiment()
    ko_fit <- fitKORandomSlopeModel(x, variable = "condition")
    ko_domain_fit <- fitKOGenomeGroupMetaAnalysis(ko_fit, x, group = "domain")
    compareKOGenomeGroups(
        ko_domain_fit,
        reference = "Bacteria",
        contrast = "Archaea"
    )
}

}
