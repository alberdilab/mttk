% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKExperiment-modeling.R
\name{fitModuleRandomSlopeModel}
\alias{fitModuleRandomSlopeModel}
\title{Fit a Module-Level Random-Slope Mixed Model}
\usage{
fitModuleRandomSlopeModel(
  x,
  variable,
  assay = "rna_gene_counts",
  libraryOffset = TRUE,
  genomeOffset = NULL,
  membershipMode = c("duplicate", "exclusive"),
  libSize = NULL,
  genomeAssay = "dna_genome_counts",
  offsetPseudocount = 1,
  keepFits = FALSE
)
}
\arguments{
\item{x}{An \code{MTTKExperiment}.}

\item{variable}{A single sample-level column name from \code{colData(x)}. The
column must be numeric or a factor with exactly two levels.}

\item{assay}{Gene-level assay name used as the RNA response.}

\item{libraryOffset}{Logical; if \code{TRUE}, include \code{offset(log(lib_size))}.}

\item{genomeOffset}{Logical; if \code{TRUE}, include
\code{offset(log(genome_abundance + offsetPseudocount))}. If \code{NULL} (the
default), MTTK uses genome-abundance normalization when \code{genomeAssay} is
available in \code{genomeExperiment(x)}.}

\item{membershipMode}{How to handle many-to-many set memberships.
\code{"duplicate"} carries a gene's RNA into every mapped module, while
\code{"exclusive"} keeps only uniquely mapped genes.}

\item{libSize}{Library-size offset specification. Use \code{NULL} to compute
\code{colSums(rnaGeneCounts(x))}, a single \code{colData(x)} column name, or a
numeric vector with one value per sample.}

\item{genomeAssay}{Genome-level assay used when \code{genomeOffset = TRUE}.}

\item{offsetPseudocount}{Non-negative pseudocount added to genome abundance
before log-offset calculation.}

\item{keepFits}{Logical; if \code{TRUE}, store the backend \code{glmmTMB} model objects
in the returned \code{MTTKFit}.}
}
\value{
An \code{MTTKFit} with one row per module. The returned fit also stores
module-by-genome conditional effects that can be extracted with
\code{\link[=moduleGenomeEffects]{moduleGenomeEffects()}}.
}
\description{
\code{fitModuleRandomSlopeModel()} fits one negative-binomial mixed model per
module using \code{glmmTMB}, with both a genome random intercept and a
genome-specific random slope for the selected sample variable.
}
\details{
Gene-level RNA counts are first aggregated to module-within-genome
observations by following \code{gene_to_ko -> ko_to_module}. The fitted model then
estimates an overall module-level effect together with genome-specific
deviations around that module effect.

This workflow is intended for the question: does the same module show similar
or heterogeneous condition-associated responses across genomes? For the
higher-level question of whether KO-level module members show a coherent
direction of change without re-aggregating counts, use
\code{\link[=fitModuleMetaAnalysis]{fitModuleMetaAnalysis()}} instead.

\code{membershipMode = "duplicate"} answers that question in terms of all RNA
assigned to the module, even if the same KO contributes to multiple modules.
\code{membershipMode = "exclusive"} instead restricts the analysis to uniquely
assigned memberships.

Genome-specific conditional module effects can be extracted with
\code{\link[=moduleGenomeEffects]{moduleGenomeEffects()}}.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeExampleMTTKExperiment()
    fit <- fitModuleRandomSlopeModel(x, variable = "condition")
    fit
    moduleGenomeEffects(fit)
}

}
