% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit.R
\name{annotateKOFit}
\alias{annotateKOFit}
\title{Join KO Annotations Onto a Fit}
\usage{
annotateKOFit(
  x,
  object,
  path = c("ko_to_module", "module_to_pathway"),
  collapse = ";"
)
}
\arguments{
\item{x}{An \code{MTTKFit} whose rows represent KO identifiers.}

\item{object}{An \code{MTTKExperiment} supplying the annotation link tables.}

\item{path}{Character vector naming one or more annotation link tables to
traverse.}

\item{collapse}{String used to combine multiple target identifiers per KO.}
}
\value{
An \code{MTTKFit} with appended annotation columns.
}
\description{
\code{annotateKOFit()} appends KO-centered annotation columns from an
\code{MTTKExperiment} onto an \code{MTTKFit}. This is intended for fits whose rows
represent KO identifiers, such as the first KO-level mixed-model workflow in
MTTK.
}
\details{
By default the function follows \code{ko_to_module -> module_to_pathway} and adds
\code{module_id} and \code{pathway_id} columns. When a KO maps to multiple downstream
identifiers, the unique values are collapsed into a single string.
}
\examples{
fit <- MTTKFit(
    results = data.frame(
        ko_id = c("K03043", "K02111"),
        estimate = c(0.3, -1.2),
        row.names = c("K03043", "K02111")
    )
)
x <- makeExampleMTTKExperiment()
annotateKOFit(fit, x)

}
