% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKExperiment-analysis.R
\name{aggregateByLink}
\alias{aggregateByLink}
\title{Aggregate Assays Along a Link Path}
\usage{
aggregateByLink(x, path, assays = NULL, fun = c("sum", "mean"))
}
\arguments{
\item{x}{An \code{MTTKExperiment}.}

\item{path}{Character vector naming one or more link tables stored in
\code{links(x)}.}

\item{assays}{Character vector of assay names to aggregate. The default uses
all assays in \code{x}.}

\item{fun}{Aggregation function. Supported values are \code{"sum"} and \code{"mean"}.}
}
\value{
A \code{SummarizedExperiment} with aggregated assays, copied \code{colData},
aggregated \code{rowData}, and provenance stored in \code{metadata()}.
}
\description{
\code{aggregateByLink()} aggregates one or more gene-level assays in an
\code{MTTKExperiment} along one or more explicit link tables. This makes it
possible to summarize RNA measurements to genomes, KOs, modules, pathways,
or other mapped feature groups.
}
\details{
The current implementation expects each link table in the path to be ordered
as a two-column mapping from source IDs to target IDs. Those first two
columns should use explicit identifier names such as \code{gene_id}, \code{ko_id}, or
\code{module_id}. For example, the packaged example data can be aggregated from
genes to modules with
\code{path = c("gene_to_ko", "ko_to_module")}. The special path
\code{"gene_to_genome"} is resolved from the authoritative
\code{rowData(x)$genome_id} mapping, so it does not require a stored
\code{links(x)[["gene_to_genome"]]} table.
}
\examples{
x <- makeExampleMTTKExperiment()

aggregateByLink(x, path = "gene_to_genome")
aggregateByLink(x, path = c("gene_to_ko", "ko_to_module"))

}
