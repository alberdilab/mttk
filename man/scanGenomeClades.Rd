% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit-phylogeny.R
\name{scanGenomeClades}
\alias{scanGenomeClades}
\title{Scan a Genome Phylogeny for Response-Shifted Clades}
\usage{
scanGenomeClades(
  x,
  object,
  tree = NULL,
  alternative = c("two.sided", "greater", "less"),
  nPerm = 999L,
  minTips = 2L,
  maxTips = NULL,
  status = "ok"
)
}
\arguments{
\item{x}{A genome-level \code{MTTKFit}, typically returned by \code{\link[=fitGenomeModel]{fitGenomeModel()}}.}

\item{object}{An \code{MTTKExperiment} supplying \code{genomeTree(object)}.}

\item{tree}{Optional rooted \code{ape::phylo} object. When \code{NULL},
\code{genomeTree(object)} is used.}

\item{alternative}{Alternative hypothesis for the permutation p-value.}

\item{nPerm}{Number of tip-label permutations used to estimate the p-value.}

\item{minTips}{Minimum subtree size to test.}

\item{maxTips}{Optional maximum subtree size to test.}

\item{status}{Optional genome-fit status filter applied before the clade
scan. The default keeps only genome rows with \code{status == "ok"}.}
}
\value{
An \code{MTTKFit} with one row per tested clade.
}
\description{
\code{scanGenomeClades()} follows \code{\link[=fitGenomeModel]{fitGenomeModel()}} with a subtree scan. Each
internal clade in the rooted genome phylogeny is tested for a shifted mean
fitted response relative to the complement of that clade.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeShowcaseMTTKExperiment()
    genome_fit <- fitGenomeModel(x, variable = "condition")
    scanGenomeClades(genome_fit, x, nPerm = 99, minTips = 2)
}

}
