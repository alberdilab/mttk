% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKExperiment-modeling-ko.R
\name{makeKOMixedModelData}
\alias{makeKOMixedModelData}
\title{Build a KO Mixed-Model Data Table}
\usage{
makeKOMixedModelData(
  x,
  variable = NULL,
  formula = NULL,
  term = NULL,
  assay = "rna_gene_counts",
  libraryOffset = TRUE,
  genomeOffset = NULL,
  libSize = NULL,
  sampleBlock = NULL,
  genomeAssay = "dna_genome_counts",
  offsetPseudocount = 1,
  referenceLevels = NULL
)
}
\arguments{
\item{x}{An \code{MTTKExperiment}.}

\item{variable}{A single sample-level column name from \code{colData(x)} for the
simple one-variable interface. The column must be numeric or a factor with
at least two levels. For factors with three or more levels supply \code{term} to
select which contrast to test. Supply exactly one of \code{variable} or \code{formula}.}

\item{formula}{Optional one-sided or two-sided fixed-effect formula for the
sample-level covariates, for example \code{~ condition + pH} or
\code{rna_count ~ condition + pH}. Offsets and genome-level random effects are
added internally by MTTK. Supply exactly one of \code{variable} or \code{formula}.}

\item{term}{Optional character string naming the model term to test. Required
when \code{variable} refers to a factor with three or more levels (supply the
contrast name, e.g. \code{"conditionB"}) or when \code{formula} defines more than one
tested term.}

\item{assay}{Gene-level assay name used as the RNA response.}

\item{libraryOffset}{Logical; if \code{TRUE}, include \code{offset(log(lib_size))}.}

\item{genomeOffset}{Logical; if \code{TRUE}, include
\code{offset(log(genome_abundance + offsetPseudocount))}. If \code{NULL} (the
default), MTTK uses genome-abundance normalization when \code{genomeAssay} is
available in \code{genomeExperiment(x)}.}

\item{libSize}{Library-size offset specification. Use \code{NULL} to compute
\code{colSums(rnaGeneCounts(x))}, a single \code{colData(x)} column name, or a
numeric vector with one value per sample.}

\item{sampleBlock}{Optional sample-level blocking variable from
\code{colData(x)}. When supplied, MTTK adds a random intercept
\code{(1 | sample_block)} to account for repeated measures or paired samples.}

\item{genomeAssay}{Genome-level assay used when \code{genomeOffset = TRUE}.}

\item{offsetPseudocount}{Non-negative pseudocount added to genome abundance
before log-offset calculation.}

\item{referenceLevels}{Optional named list or named character vector setting
the reference levels of factor-like sample covariates before model fitting.}
}
\value{
An \code{S4Vectors::DataFrame} with one row per KO/genome/sample
observation.
}
\description{
\code{makeKOMixedModelData()} materializes the long-form observation table used by
\code{\link[=fitKOMixedModel]{fitKOMixedModel()}}. Each row corresponds to one KO/genome/sample
observation, with RNA counts, sample-level covariates, and optional
genome-abundance offsets aligned and ready for model fitting.
}
\details{
The returned table is useful for inspecting the modeled data before fitting,
or for reusing the same KO/genome aggregation in custom workflows.
}
\examples{
x <- makeExampleMTTKExperiment()
model_data <- makeKOMixedModelData(x, variable = "condition")
model_data

}
