% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKExperiment-modeling.R
\name{makeKOMixedModelData}
\alias{makeKOMixedModelData}
\title{Build a KO Mixed-Model Data Table}
\usage{
makeKOMixedModelData(
  x,
  variable,
  model = c("library_only", "library_plus_genome_abundance"),
  assay = "rna_gene_counts",
  libSize = NULL,
  genomeAssay = "dna_genome_counts",
  offsetPseudocount = 1
)
}
\arguments{
\item{x}{An \code{MTTKExperiment}.}

\item{variable}{A single sample-level column name from \code{colData(x)}. The
column must be numeric or a factor with exactly two levels.}

\item{model}{Which of the two supported mixed-model specifications to
prepare.}

\item{assay}{Gene-level assay name used as the RNA response.}

\item{libSize}{Library-size offset specification. Use \code{NULL} to compute
\code{colSums(rnaGeneCounts(x))}, a single \code{colData(x)} column name, or a
numeric vector with one value per sample.}

\item{genomeAssay}{Genome-level assay used when
\code{model = "library_plus_genome_abundance"}.}

\item{offsetPseudocount}{Non-negative pseudocount added to genome abundance
before log-offset calculation.}
}
\value{
An \code{S4Vectors::DataFrame} with one row per KO/genome/sample
observation.
}
\description{
\code{makeKOMixedModelData()} materializes the long-form observation table used by
\code{\link[=fitKOMixedModel]{fitKOMixedModel()}}. Each row corresponds to one KO/genome/sample
observation, with RNA counts, sample-level covariates, and optional
genome-abundance offsets aligned and ready for model fitting.
}
\details{
The returned table is useful for inspecting the modeled data before fitting,
or for reusing the same KO/genome aggregation in custom workflows.
}
\examples{
x <- makeExampleMTTKExperiment()
model_data <- makeKOMixedModelData(x, variable = "condition")
head(as.data.frame(model_data))
S4Vectors::metadata(model_data)$mttk_ko_model$effectLabel

}
