% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit-meta-analysis.R
\name{fitKOGenomeGroupMetaAnalysis}
\alias{fitKOGenomeGroupMetaAnalysis}
\title{Meta-Analyze Genome-Specific KO Effects by a Genome Metadata Group}
\usage{
fitKOGenomeGroupMetaAnalysis(
  x,
  object,
  group,
  method = c("random", "fixed"),
  minGenomes = 1L,
  status = "ok",
  collapse = ";"
)
}
\arguments{
\item{x}{A KO random-slope \code{MTTKFit}, from \code{\link[=fitKORandomSlopeModel]{fitKORandomSlopeModel()}}.}

\item{object}{An \code{MTTKExperiment} supplying genome metadata.}

\item{group}{A single column name from \code{genomeData(object)} used to group
genomes, such as \code{"domain"} or \code{"clade"}.}

\item{method}{Meta-analysis method. \code{"random"} uses inverse-variance
random-effects meta-analysis with a DerSimonian-Laird \code{tau^2} estimate,
while \code{"fixed"} uses fixed-effect inverse-variance pooling.}

\item{minGenomes}{Minimum number of tested genome-specific KO effects
required for a KO/group combination to receive an \code{"ok"} result.}

\item{status}{Optional KO-fit status filter applied before the meta-analysis.
The default keeps only KO rows with \code{status == "ok"}. Use \code{NULL} to keep
all KO rows.}

\item{collapse}{String used to combine genome identifiers in the
\code{member_genome_ids} and \code{tested_genome_ids} columns.}
}
\value{
An \code{MTTKFit} with one row per KO/group combination.
}
\description{
\code{fitKOGenomeGroupMetaAnalysis()} follows a KO random-slope model with a
group-level synthesis step. It starts from the genome-specific KO effects
extracted by \code{\link[=koGenomeEffects]{koGenomeEffects()}} and summarizes those effects within a genome
grouping variable stored in \code{genomeData(x)}, such as \code{domain}, \code{clade}, or
another taxonomy/phylogeny-derived label.
}
\details{
This workflow is intended for the question: for a given KO, do genomes in a
taxonomic or clade-defined group show a coherent response, and how
heterogeneous are those genome-specific KO responses within the group?

The input \code{x} must be an \code{MTTKFit} returned by \code{\link[=fitKORandomSlopeModel]{fitKORandomSlopeModel()}}.
The meta-analysis operates on the genome-specific conditional KO effects
stored in that fit, not on the KO-wide fixed effect. By default a
random-effects inverse-variance meta-analysis with a DerSimonian-Laird
\code{tau^2} estimate is used.

The per-genome standard errors are approximate. MTTK derives them from the
fixed-effect variance and the conditional random-slope variance, without
modeling the full cross-covariance between those components. As a result,
this workflow should be interpreted as an approximate synthesis layer for
taxonomy-aware KO follow-up rather than as fully exact hierarchical
inference.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeExampleMTTKExperiment()
    ko_fit <- fitKORandomSlopeModel(x, variable = "condition")
    ko_domain_fit <- fitKOGenomeGroupMetaAnalysis(ko_fit, x, group = "domain")
    fitTable(ko_domain_fit, sortBy = "q_value")
}

}
