% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit.R
\name{fetchKEGGModuleTable}
\alias{fetchKEGGModuleTable}
\title{Fetch KEGG Module Annotations with KEGGREST}
\usage{
fetchKEGGModuleTable(moduleIds = NULL, batchSize = 10L, sleep = 0)
}
\arguments{
\item{moduleIds}{Optional character vector of KEGG module identifiers. Bare
IDs such as \code{M00001} and prefixed IDs such as \code{md:M00001} are both
accepted. Values collapsed with \code{";"} are also split automatically.}

\item{batchSize}{Number of module entries to request per \code{keggGet()} call.
KEGG limits \code{keggGet()} requests to at most 10 entries.}

\item{sleep}{Optional number of seconds to wait between successive KEGG REST
requests.}
}
\value{
An \code{S4Vectors::DataFrame}.
}
\description{
\code{fetchKEGGModuleTable()} retrieves KEGG module metadata directly from the
KEGG MODULE database through \code{KEGGREST} and returns it in the standardized
format used by mttk.
}
\details{
The resulting table contains \code{module_id}, \code{module_name}, \code{module_class}, and
\code{module_definition} columns and can be supplied directly to
\code{\link[=annotateKOFit]{annotateKOFit()}} through its \code{moduleTable} argument.

By default the function requests the full KEGG module catalog. To reduce
runtime and network traffic, pass a subset of \code{moduleIds} when only a small
number of modules are needed.

Access to the KEGG REST API is provided by \code{KEGGREST} and subject to KEGG's
academic-use terms.
}
\examples{
\dontrun{
module_table <- fetchKEGGModuleTable(c("M00001", "M00002"))
}

}
