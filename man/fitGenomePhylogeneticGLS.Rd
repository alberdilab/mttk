% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit-phylogeny.R
\name{fitGenomePhylogeneticGLS}
\alias{fitGenomePhylogeneticGLS}
\title{Fit a Genome-Level Phylogenetic GLS Model}
\usage{
fitGenomePhylogeneticGLS(
  x,
  object,
  tree = NULL,
  method = c("REML", "ML"),
  useWeights = TRUE,
  status = "ok",
  keepFits = FALSE
)
}
\arguments{
\item{x}{A genome-level \code{MTTKFit}, typically returned by \code{\link[=fitGenomeModel]{fitGenomeModel()}}.}

\item{object}{An \code{MTTKExperiment} supplying \code{genomeTree(object)}.}

\item{tree}{Optional \code{ape::phylo} object. When \code{NULL}, \code{genomeTree(object)}
is used.}

\item{method}{Fitting method passed to \code{nlme::gls()}.}

\item{useWeights}{Logical; if \code{TRUE}, the phylogenetic GLS uses the fitted
effect standard errors as inverse-variance weights.}

\item{status}{Optional genome-fit status filter applied before fitting. The
default keeps only genome rows with \code{status == "ok"}.}

\item{keepFits}{Logical; should the backend \code{gls} fit object be stored in the
returned \code{MTTKFit}?}
}
\value{
An \code{MTTKFit} with one row summarizing the phylogenetically corrected
mean genome response.
}
\description{
\code{fitGenomePhylogeneticGLS()} fits an intercept-only phylogenetic GLS model to
the genome-level fitted response estimates returned by \code{\link[=fitGenomeModel]{fitGenomeModel()}}.
The covariance among genomes is defined by the rooted genome phylogeny under
a Brownian-motion model.
}
\details{
This workflow is useful when the question is "what is the phylogenetically
corrected mean genome response?" rather than "is there overall phylogenetic
signal?" The response is the fitted genome-level effect estimate, not the raw
RNA count.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeShowcaseMTTKExperiment()
    genome_fit <- fitGenomeModel(x, variable = "condition")
    fitGenomePhylogeneticGLS(genome_fit, x)
}

}
