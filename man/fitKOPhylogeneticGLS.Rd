% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKFit-phylogeny.R
\name{fitKOPhylogeneticGLS}
\alias{fitKOPhylogeneticGLS}
\title{Fit a KO-Level Phylogenetic GLS Model}
\usage{
fitKOPhylogeneticGLS(
  x,
  object,
  tree = NULL,
  method = c("REML", "ML"),
  useWeights = TRUE,
  minGenomes = 3L,
  status = "ok",
  keepFits = FALSE
)
}
\arguments{
\item{x}{A KO random-slope \code{MTTKFit}, from \code{\link[=fitKORandomSlopeModel]{fitKORandomSlopeModel()}}.}

\item{object}{An \code{MTTKExperiment} supplying \code{genomeTree(object)}.}

\item{tree}{Optional \code{ape::phylo} object. When \code{NULL}, \code{genomeTree(object)}
is used.}

\item{method}{Fitting method passed to \code{nlme::gls()}.}

\item{useWeights}{Logical; if \code{TRUE}, the phylogenetic GLS uses the fitted
effect standard errors as inverse-variance weights.}

\item{minGenomes}{Minimum number of genomes with finite feature effects
required for a KO to receive an \code{"ok"} result.}

\item{status}{Optional genome-fit status filter applied before fitting. The
default keeps only genome rows with \code{status == "ok"}.}

\item{keepFits}{Logical; should the backend \code{gls} fit object be stored in the
returned \code{MTTKFit}?}
}
\value{
An \code{MTTKFit} with one row per KO.
}
\description{
\code{fitKOPhylogeneticGLS()} fits one intercept-only phylogenetic GLS model per
KO using the genome-specific KO effects returned by \code{\link[=fitKORandomSlopeModel]{fitKORandomSlopeModel()}}.
The covariance among genomes is defined by the rooted genome phylogeny under
a Brownian-motion model.
}
\details{
This workflow is useful when the question is "what is the phylogenetically
corrected mean KO response across genomes?" rather than whether there is
overall phylogenetic signal or a localized responding clade.
}
\examples{
if (requireNamespace("glmmTMB", quietly = TRUE)) {
    x <- makeShowcaseMTTKExperiment()
    ko_fit <- fitKORandomSlopeModel(x, variable = "condition")
    fitKOPhylogeneticGLS(ko_fit, x)
}

}
