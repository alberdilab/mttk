% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKExperiment-modeling.R
\name{aggregateToPathwayGenome}
\alias{aggregateToPathwayGenome}
\title{Aggregate Gene RNA to Pathway-within-Genome Counts}
\usage{
aggregateToPathwayGenome(
  x,
  assay = "rna_gene_counts",
  membershipMode = c("duplicate", "exclusive")
)
}
\arguments{
\item{x}{An \code{MTTKExperiment}.}

\item{assay}{A single gene-level assay name to aggregate.}

\item{membershipMode}{How to handle many-to-many set memberships.
\code{"duplicate"} carries a gene's RNA into every mapped pathway, while
\code{"exclusive"} keeps only uniquely mapped genes.}
}
\value{
A \code{SummarizedExperiment} with one row per pathway/genome pair and one
column per sample.
}
\description{
\code{aggregateToPathwayGenome()} collapses a gene-level assay to pathway/genome
pairs by following the functional path \code{gene_to_ko -> ko_to_pathway}. The
returned rows represent pathway-within-genome observations that can be used
in pathway-level mixed models.
}
\details{
Because KO-to-pathway membership can be many-to-many, the same gene can
contribute to multiple pathways. \code{membershipMode = "duplicate"} carries the
full count into each mapped pathway/genome observation and is appropriate
for the question "how much RNA is assigned to this pathway?". In contrast,
\code{membershipMode = "exclusive"} keeps only genes whose final pathway mapping
is unique, which is more conservative and avoids duplicated counts entirely.
}
\examples{
x <- makeExampleMTTKExperiment()
pathway_genome <- aggregateToPathwayGenome(x)
pathway_genome
SummarizedExperiment::rowData(pathway_genome)[, c("pathway_id", "genome_id")]

}
