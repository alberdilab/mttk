% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MTTKExperiment-modeling.R
\name{aggregateToModuleGenome}
\alias{aggregateToModuleGenome}
\title{Aggregate Gene RNA to Module-within-Genome Counts}
\usage{
aggregateToModuleGenome(
  x,
  assay = "rna_gene_counts",
  membershipMode = c("duplicate", "exclusive")
)
}
\arguments{
\item{x}{An \code{MTTKExperiment}.}

\item{assay}{A single gene-level assay name to aggregate.}

\item{membershipMode}{How to handle many-to-many set memberships.
\code{"duplicate"} carries a gene's RNA into every mapped module, while
\code{"exclusive"} keeps only uniquely mapped genes.}
}
\value{
A \code{SummarizedExperiment} with one row per module/genome pair and one
column per sample.
}
\description{
\code{aggregateToModuleGenome()} collapses a gene-level assay to module/genome
pairs by following the functional path \code{gene_to_ko -> ko_to_module}. The
returned rows represent module-within-genome observations that can be used in
module-level mixed models.
}
\details{
Because KO-to-module membership can be many-to-many, the same gene can
contribute to multiple modules. \code{membershipMode = "duplicate"} carries the
full count into each mapped module/genome observation and is appropriate for
the question "how much RNA is assigned to this module?". In contrast,
\code{membershipMode = "exclusive"} keeps only genes whose final module mapping is
unique, which is more conservative and avoids duplicated counts entirely.
}
\examples{
x <- makeExampleMTTKExperiment()
module_genome <- aggregateToModuleGenome(x)
module_genome
SummarizedExperiment::rowData(module_genome)[, c("module_id", "genome_id")]

}
